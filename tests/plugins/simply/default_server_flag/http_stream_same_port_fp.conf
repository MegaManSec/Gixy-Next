http {
    server {
        listen 8080;
        server_name a.test;
    }

    server {
        listen 80 default_server;
        server_name b.test;
    }
}

stream {
    server {
        listen 8080 udp;
        proxy_pass backend;
    }
}

# Should TRIGGER: the real allowlist is dead code. ngx_http_access_inet()
# stops at the first matching rule, so `allow all` answers for every client
# the /8 did not cover and `deny all` never applies. Verified against nginx:
# this returns 200 to an arbitrary client.
location /status {
    stub_status;
    allow 10.0.0.0/8;
    allow all;
    deny all;
}

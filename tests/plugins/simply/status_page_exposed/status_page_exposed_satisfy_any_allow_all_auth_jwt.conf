# Should TRIGGER: auth_jwt runs in the access phase like auth_basic, so
# under `satisfy any` the universal allow makes the access module return
# NGX_OK and the JWT check never runs.
location /status {
    satisfy any;
    allow all;
    auth_jwt "x";
    auth_jwt_key_file /etc/nginx/keys.jwk;
    stub_status;
}

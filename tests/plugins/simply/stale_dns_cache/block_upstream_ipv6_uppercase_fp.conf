# Should NOT trigger: bracketed IPv6 literals using uppercase hex digits.
http {
  upstream svc {
    server [2001:DB8::DEAD:BEEF]:443;
  }

  server {
    listen 80;
    location / {
      proxy_pass http://svc;
    }
  }
}

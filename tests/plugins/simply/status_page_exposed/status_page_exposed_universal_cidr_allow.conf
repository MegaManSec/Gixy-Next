# Should TRIGGER: `allow 0.0.0.0/0` matches every IPv4 client before the
# `deny all` is ever reached, so the endpoint is open. Verified against
# nginx: this returns 200 to an arbitrary client.
location /status {
    stub_status;
    allow 0.0.0.0/0;
    deny all;
}

location / {
  set $uri_suffix /foo;
  rewrite ^ $request_uri; # Sets the $1/$uri variable to the raw path
  proxy_pass http://backend/$uri_suffix; # $uri_suffix is not $uri, path still gets double-encoded
}

http {
    add_trailer_inherit merge;

    server {
        server_name example.com;

        add_trailer X-Checksum "parent";

        location / {
            add_trailer X-Request-Id "child";
        }
    }
}

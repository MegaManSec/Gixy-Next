# Should NOT trigger: stub_status is protected by auth_oidc, which the
# `satisfy` directive lists alongside auth_basic, auth_request and auth_jwt
# as an access-phase authentication module.
location = /nginx_status {
  stub_status on;
  auth_oidc idp;
}

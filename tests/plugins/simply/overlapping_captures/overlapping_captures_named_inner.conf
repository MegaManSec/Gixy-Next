# Edge case: the inner capture is named and the outer is unnamed —
# symmetric to the named-outer case. PCRE assigns $1 to the outer and $2
# to the named inner; both ranges still overlap.
server {
    location / {
        rewrite ^/((?<inner>.*))$ http://example.com/$1$2 redirect;
    }
}

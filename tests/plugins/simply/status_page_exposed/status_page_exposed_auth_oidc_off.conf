# Should TRIGGER: `auth_oidc off` cancels the inherited directive, so the
# status page is left unauthenticated.
server {
  auth_oidc idp;
  location = /nginx_status {
    stub_status on;
    auth_oidc off;
  }
}

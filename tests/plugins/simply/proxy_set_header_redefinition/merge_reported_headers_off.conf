# Options: {"headers": [], "merge_reported_headers": false}
http {
    proxy_set_header Host $host;

    server {
        server_name example.com;

        proxy_set_header X-Real-IP $remote_addr;

        location /internal {
            proxy_set_header X-Real-IP $remote_addr;
            proxy_pass http://backend;
        }
    }
}

http {
    server {
        listen 80 default_server;
        server_name a.test;
    }
}

stream {
    server {
        listen 12345;
        proxy_pass backend_a;
    }

    server {
        listen 12345;
        proxy_pass backend_b;
    }
}

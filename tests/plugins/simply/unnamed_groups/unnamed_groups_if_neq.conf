# Positive: `if ($var != VALUE)` — same complex_value_code path
# (rewrite_module.c:735).
server {
    location / {
        rewrite ^(.*) /new?c=1;
        if ($host != "/$1") {
            return 200 ok;
        }
    }
}

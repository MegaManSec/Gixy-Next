# Should NOT trigger: upstream uses a bracketed IPv6 literal without a port.
http {
  upstream v6_service {
    server [2001:db8::1];
  }

  server {
    listen 80;
    location / {
      proxy_pass http://v6_service;
    }
  }
}

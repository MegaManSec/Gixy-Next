# Should NOT trigger: try_files serves the response in the content phase,
# which runs after the access phase, so allow/deny are enforced.
# Verified against nginx: a denied client receives 403.
location /admin/ {
    allow 127.0.0.1;
    deny all;

    root /var/www/admin;
    try_files /index.html =404;
}

http {
    map $arg_key $mapped {
        default "/d/";
        "a" "/a/";
        "b" "/b/";
    }

    server {
        listen 80;

        location / {
            proxy_pass http://$mapped$arg_host;
        }
    }
}

server {
    location = /.well-known/security.txt {
        return 301 https://$canonical_host/.well-known/security.txt;
    }
}

map $ssl_server_name $canonical_host {
    default $ssl_server_name;
}

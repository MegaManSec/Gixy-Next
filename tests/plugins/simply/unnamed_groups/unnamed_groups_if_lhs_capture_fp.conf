# False positive guard: as with `if ($1)`, a numeric capture cannot be
# the tested operand of a comparison — nginx rejects this config with
# `[emerg] unknown "1" variable`.
server {
    location / {
        rewrite ^(.*) /new?c=1;
        if ($1 = "x") {
            return 200 ok;
        }
    }
}

# Should NOT trigger: stub_status is protected by auth_jwt, which the
# `satisfy` directive lists alongside auth_basic and auth_request as an
# access-phase authentication module.
location = /nginx_status {
  stub_status on;
  auth_jwt "metrics";
  auth_jwt_key_file /etc/nginx/keys.jwk;
}

# Edge case: same as the http:// case but with https://. Both prefixes
# implicitly set regex->redirect = 1; the original PR only matched on the
# explicit `redirect`/`permanent` flag and missed this shape.
server {
    location / {
        rewrite ^/((.*))$ https://example.com/$1$2;
    }
}

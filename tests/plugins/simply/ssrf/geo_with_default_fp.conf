http {
    geo $prefix {
        default "/default/";
        10.0.0.0/8 "/internal/";
    }

    server {
        listen 80;

        location / {
            proxy_pass http://$prefix$arg_host;
        }
    }
}

# Options: {"headers": ["host"], "merge_reported_headers": true}
http {
    server {
        server_name example.com;

        proxy_set_header X-Request-Start $msec;

        location /internal {
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_pass http://backend;
        }
    }
}

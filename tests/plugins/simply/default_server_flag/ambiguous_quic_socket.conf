http {
    server {
        listen 443 ssl default_server;
        listen 443 quic;
        server_name a.test;
    }

    server {
        listen 443 ssl;
        listen 443 quic;
        server_name b.test;
    }
}

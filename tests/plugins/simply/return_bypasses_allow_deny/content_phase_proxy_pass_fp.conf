# Should NOT trigger: proxy_pass is a content-phase handler, so allow/deny
# are enforced. Verified against nginx: a denied client receives 403.
location /admin/ {
    allow 127.0.0.1;
    deny all;

    proxy_pass http://admin_backend;
}
